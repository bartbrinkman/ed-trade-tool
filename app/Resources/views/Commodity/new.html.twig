{% extends 'base.html.twig' %}

{% block body %}
    <h3>New commodity</h3>
	{{ form_start(form) }}
	    {{ form_widget(form) }}
	    <div class="form-group">
		    <div>
		    	<button type="submit" class="btn btn-primary">Create</button>
		    	<a href="{{ path('commodity-index') }}" class="btn btn-link">Cancel</a>
		    </div>
		</div>
	{{ form_end(form) }}
{% endblock %}
