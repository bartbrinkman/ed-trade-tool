{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit system</h1>
	{{ form_start(form) }}
	    {{ form_widget(form) }}
	    <input type="submit" value="Save" class="btn btn-default pull-right" />
	{{ form_end(form) }}
{% endblock %}
