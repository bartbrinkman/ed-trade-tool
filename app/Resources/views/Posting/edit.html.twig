{% extends 'base.html.twig' %}

{% block body %}
    <h3>Edit posting</h3>
	{{ form_start(form) }}
	    {{ form_widget(form) }}
	    <input type="submit" value="Save" class="btn btn-primary" />
	{{ form_end(form) }}
{% endblock %}
